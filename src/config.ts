/**
 * Feature flag: clubs feature.
 *
 * Strava API Agreement §6.3 restricts club data access to apps that qualify
 * for the Community Application exception (non-commercial, under 10 000 users).
 * Set to `false` to disable the clubs feature app-wide if that exception no
 * longer applies.
 */
export const CLUBS_ENABLED = true
